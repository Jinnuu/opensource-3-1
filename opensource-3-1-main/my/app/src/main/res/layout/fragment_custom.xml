<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#F6F7FA"
    android:padding="0dp">

    <!-- 상단 타이틀 -->
    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="커스텀"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="#222222"
        android:paddingTop="24dp"
        android:paddingBottom="16dp"
        android:paddingStart="24dp"
        android:paddingEnd="24dp" />

    <!-- 카드 형태의 메인 컨테이너 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="vertical"
        android:background="@drawable/rounded_white_bg"
        android:layout_margin="16dp"
        android:padding="0dp">

        <!-- 운동 이미지 -->
        <ImageView
            android:id="@+id/ivCustomImage"
            android:layout_width="255dp"
            android:layout_height="241dp"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="40dp"
            android:src="@drawable/custom_image" />

        <!-- 메인 안내 텍스트 -->
        <TextView
            android:id="@+id/tvMainGuide"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="첫 번째 커스텀 운동을 만드세요"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="#222222"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="32dp" />

        <!-- 서브 안내 텍스트 -->
        <TextView
            android:id="@+id/tvSubGuide"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="나만의 루틴을 설정하세요"
            android:textSize="15sp"
            android:textColor="#888888"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="32dp" />

        <!-- 시작 버튼 -->
        <Button
            android:id="@+id/btnStart"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:text="+ 시작"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="#FFFFFF"
            android:backgroundTint="#246BFF"
            android:layout_marginStart="24dp"
            android:layout_marginEnd="24dp"
            android:layout_marginBottom="40dp" />
    </LinearLayout>

    <!-- 하단 네비게이션 바는 별도의 xml(nav)로 관리하는 것이 일반적입니다 -->
</LinearLayout>
